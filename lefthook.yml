pre-commit:
  parallel: true
    commands:
      check:
        glob: "*.{js,ts,jsx,tsx,astro}"
        run: | 
          pnpm eslint
          pnpm prettier --check .

commit-msg:
  commands:
    commitlint:
      run: pnpm --no -- commitlint --edit "$1"